[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: Unit tests (fast, no external dependencies)
    integration: Integration tests (requires DB, may use LLM)
    e2e: End-to-end tests (full system tests)
    slow: Slow tests (skip with -m "not slow")
    performance: Performance benchmarks (measures latency, cache effectiveness)
    requires_neo4j: Tests requiring Neo4j connection
    requires_openai: Tests requiring OpenAI API key
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
